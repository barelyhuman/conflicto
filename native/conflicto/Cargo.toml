[package]
name = "conflicto"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Conflicto — native git diff viewer (GPUI)"

[[bin]]
name = "conflicto"
path = "src/main.rs"

[dependencies]
conflicto_core = { workspace = true }
gpui = "0.2.2"
rfd = { workspace = true }
